/**
 * Token di dimensione ri-esportati per comodità in tutti i componenti.
 *
 * Preferisci usare direttamente `StandardButton` / `ActionButton` con la prop `size`,
 * ma quando devi costruire manualmente un bottone (es. span con stato, badge, ecc.),
 * usa questi token per garantire allineamento dimensionale tra pulsanti affiancati.
 */
export {
  BTN_SIZE_XL,
  BTN_SIZE_MD,
  BTN_SIZE_SM,
  BTN_SIZE_XS,
} from '@/components/ui/standard-button-classes'
