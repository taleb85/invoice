# Copia in `.env.local` e compila i valori. Non committare segreti.
#
# Multi-cliente (stessa app, ambienti separati)
# ───────────────────────────────────────────
# Ogni cliente commerciale ha il PROPRIO progetto Supabase e il PROPRIO progetto Vercel.
# Environment Variables su Vercel: un set per deploy, senza riutilizzare URL/chiavi tra clienti.
# Checklist: docs/DEPLOY_PER_CLIENTE.md

# URL pubblico dell’app (metadata Open Graph / Twitter). In locale viene usato http://localhost:3000 se assente.
# NEXT_PUBLIC_SITE_URL=https://tuodominio.it

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Google AI — Gemini (OCR / estrazione documenti)
# https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Facoltativo: override modello (default gemini-2.5-flash-lite). Billing “pay-as-you-go” sullo stesso progetto della GEMINI_API_KEY (AI Studio → Usage and billing).
# GEMINI_MODEL=gemini-2.5-flash-lite

# Resend (email)
RESEND_API_KEY=

# Cron
CRON_SECRET=

# Rekki — ricerca fornitore da P.IVA (`POST /api/fornitore-rekki`, server Next.js).
# Dev: `.env.local` in root progetto. Prod: variabili d’ambiente del deploy (es. Vercel), non Supabase salvo Edge dedicata.
# REKKI_API_KEY=
# URL con placeholder {vat} (solo cifre), es. https://api.example.com/v1/suppliers?vat={vat}
# REKKI_SUPPLIERS_SEARCH_URL=

# IMAP globale (opzionale se config per sede nel DB)
# IMAP_HOST=
# IMAP_PORT=993
# IMAP_USER=
# IMAP_PASSWORD=

# Opzionale: PIN (1–12 caratteri) richiesto sulla schermata login admin prima di email/password.
# Se assente o vuoto, il gate non è attivo. Vedi `src/app/api/admin-login-gate/route.ts`.
# ADMIN_LOGIN_GATE_PIN=
